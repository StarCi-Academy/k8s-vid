apiVersion: v1
kind: Pod
metadata:
  name: server-pod
  labels:
    app: server
spec:
  containers:
    - name: server
      image: starciacademy/k8s-pod:latest
      ports:
        - containerPort: 3000