apiVersion: v1
kind: Service
metadata:
  name: postgresql-service
  labels:
    app: postgresql
spec:
  selector:
    app: postgresql
  ports:
    - port: 6969
      targetPort: 5432
  type: ClusterIP